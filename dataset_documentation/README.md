# Dataset Documentation

This folder documents the datasets generated from the custom 5G-LENA digital twin scripts in
`contrib/nr/examples/5G-LENA-digital-twin-script/`.

The documentation is organized around two base simulation scenarios:

- **Bursty traffic scenario**: generated by `cellular-network-user.cc`
- **Benchmarking traffic scenario**: generated by `delay-benchmarking-user.cc`

Here, **scenario** means the base network deployment setup and traffic mix. Each scenario consists of multiple experiment runs by changing selected parameters within those
base scenarios.

## Common 5G Network Setup

Both scenarios use the same shared 5G network setup:

- one gNB and a configurable number of UEs
- single component carrier and single bandwidth part
- 3.5 GHz carrier frequency and 40 MHz bandwidth (giving us 106 PRBs)
- numerology 1
- TDD pattern `DL|DL|DL|S|UL` repeated across the 10 ms frame
- OFDMA proportional-fair scheduler
- realistic beamforming triggered from SRS measurements
- UE uplink power control enabled

## Scenario 1: Bursty Traffic

This scenario is generated by `cellular-network-user.cc`. It is intended to create application-level
bursty uplink traffic to study the network under a realistic and challenging bursty traffic scenario. Bursty traffic is created by recreating the traffic generation pattern of various real-world XR applications.    
The batch campaign runner for this scenario is `run-parallel-bursty-traffic-sims.py`.

The campaign uses:

- 10 mobile UEs by default
- selected UEs running the bursty traffic app in uplink
- uplink and downlink delay probe apps on all UEs
- runs for 1000 seconds by default

### Bursty Traffic Experiment Runs

All runs inherit the common campaign defaults:

| Run | Parameter change from campaign default | Purpose |
|---|---|---|
| `run01` | `numUesWithVrApp=3` | Baseline bursty run, matching the campaign default |
| `run02` | `numUesWithVrApp=2` | Lower bursty traffic load |
| `run03` | `numUesWithVrApp=5` | Higher bursty traffic load |
| `run04` | `numUesWithVrApp=6` | Highest bursty traffic load in this campaign |
| `run05` | `channelScenario=InH-OfficeMixed` | Indoor channel variation |
| `run06` | `channelScenario=UMi` | Urban micro channel variation |
| `run07` | `channelScenario=UMa` | Urban macro channel variation |
| `run08` | `vrAppProfile=Minecraft` | Bursty traffic profile variation |
| `run09` | `vrAppProfile=GoogleEarthVrCities` | Bursty traffic profile variation |
| `run10` | `vrAppProfile=GoogleEarthVrTour` | Bursty traffic profile variation |

## Scenario 2: Benchmarking Traffic

This scenario is generated by `delay-benchmarking-user.cc`. It is intended to study uplink delay
components by sending controlled UDP delay probes, with optional background load.
The batch campaign runner for this scenario is `run-parallel-benchmarking-sims.py`.

The scenario differs from the bursty traffic scenario in these main ways:

- it uses fixed UE positions instead of mobile UEs
- the number of UEs is set from the load mode: 1 UE when there is no background load, 2 UEs when TCP or UDP
  background load is enabled
- the UE that does not send any backgropund load sends uplink and downlink delay probes 

### Benchmarking Traffic Experiment Runs

All runs inherit the common benchmark defaults:

| Run range | Parameter sweep | Background load | Purpose |
|---|---|---|---|
| `benchmark01`-`benchmark05` | `delayPacketSize` in `{50, 100, 500, 1000, 1400}` bytes with `delayInterval=50ms` | none | Packet-size sweep without competing traffic |
| `benchmark06`-`benchmark09` | `delayPacketSize=1400` bytes with `delayInterval` in `{20ms, 25ms, 75ms, 100ms}` | none | Inter-packet interval sweep without competing traffic |
| `benchmark10`-`benchmark13` | `delayPacketSize` in `{50, 1400}` bytes and `delayInterval` in `{20ms, 50ms, 100ms}` for the listed runs | UDP CBR load 10 Mbps | Delay behavior under 25% UDP uplink load |
| `benchmark14`-`benchmark17` | `delayPacketSize` in `{50, 1400}` bytes and `delayInterval` in `{20ms, 50ms, 100ms}` for the listed runs | UDP CBR load 20 Mbps | Delay behavior under 50% UDP uplink load |
| `benchmark18`-`benchmark21` | `delayPacketSize` in `{50, 1400}` bytes and `delayInterval` in `{20ms, 50ms, 100ms}` for the listed runs | UDP CBR load 30 Mbps | Delay behavior under 75% UDP uplink load |
| `benchmark22`-`benchmark25` | `delayPacketSize` in `{50, 1400}` bytes and `delayInterval` in `{20ms, 50ms, 100ms}` for the listed runs | TCP full buffer background load | Delay behavior under TCP full buffer uplink load |

## Data Formats

Each generated dataset is available as just raw data as well as processed into datasets with different content:

| Format | Description | Documentation |
|---|---|---|
| Raw ns-3 logs | Direct trace files emitted by the simulator | [raw_ns3_data_documentation.md](raw_ns3_data_documentation.md) |
| Parsed uplink data | RAN and application level metrics from raw ns-3 trace logs parsed into uplink per-RNTI time-window aggregated CSVs | [parsed_uplink_data_documentation.md](parsed_uplink_data_documentation.md) |
| Delay decomposition data | Per-packet uplink delay components decomposition in a CSV format| [delay_decomposition_data_documentation.md](delay_decomposition_data_documentation.md) |

### Bursty Traffic Dataset Access

| Dataset | Link |
|---|---|
| Raw ns-3 data | [Google Drive](https://drive.google.com/drive/folders/1ri-lEMtLknnHW55Hqn2xiYxC8jwfg4XA?usp=sharing) |
| Parsed uplink data | [Google Drive](https://drive.google.com/file/d/1ywy0lw2J83gtebYvxh301DaVeSisEuVd/view?usp=drive_link) |
| Delay decomposition data | [Google Drive](https://drive.google.com/file/d/1kYHCwe6iTCnm6t3SVf-BxS4xI-3cIo41/view?usp=drive_link) |

### Benchmarking Traffic Dataset Access

| Dataset | Link |
|---|---|
| Raw ns-3 data | TODO: add Google Drive link |
| Parsed uplink data | [Google Drive](https://drive.google.com/file/d/1e6_v-kQ0MpCXEHgKEhAIVegrd8CeUR-W/view?usp=sharing) |
| Delay decomposition data | [Google Drive](https://drive.google.com/file/d/1l-WdW8NCmxutVLgUk2mCTcW3tKCiYWAd/view?usp=drive_link) |